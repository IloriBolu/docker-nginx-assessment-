A brief description of the project.
The exact docker build command a user would need to build the image from your Dockerfile.
The exact docker run command needed to start a container from your image. This command must map port 8888 on the host to port 80 in the container.
Commit and push all your files (Dockerfile, README.md, and the app directory) to your GitHub repository.